[workspace]
members = [
    "spectrum-node",
    "spectrum-network",
    "spectrum-crypto",
    "algebra-core"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
lto = true